CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
SET(LIB Lua)
AUX_SOURCE_DIRECTORY(. CODE)
IF(WIN32)
ADD_DEFINITIONS(-DLUA_BUILD_AS_DLL)
ELSE(WIN32)
ENDIF(WIN32)
ADD_LIBRARY(${LIB} SHARED ${CODE} ${HEAD})
