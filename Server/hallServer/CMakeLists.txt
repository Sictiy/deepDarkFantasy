CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
PROJECT(HallServer)
FILE(GLOB HEAD *.h)
set(CMAKE_CXX_FLAGS ${CMAKE_CXX_FLAGS} "-std=c++11 -pthread -lglog")
AUX_SOURCE_DIRECTORY(. CODE)
ADD_EXECUTABLE(HallServer ${CODE} ${HEAD})
LINK_DIRECTORIES("/usr/local/bin")
